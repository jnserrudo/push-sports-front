import React, { useState, useEffect } from 'react';
import { 
  ShoppingCart, 
  Search, 
  Menu, 
  X, 
  Star, 
  ChevronRight, 
  Zap, 
  Truck, 
  ShieldCheck, 
  CreditCard, 
  Instagram, 
  MessageCircle,
  Play,
  MapPin
} from 'lucide-react';

// --- DATOS DE EJEMPLO (MOCK DATA) ---

const CATEGORIAS = [
  { id: 1, name: "Proteínas", icon: "💪" },
  { id: 2, name: "Creatinas", icon: "⚡" },
  { id: 3, name: "Pre-Workouts", icon: "🔥" },
  { id: 4, name: "Vitaminas", icon: "💊" },
  { id: 5, name: "Accesorios", icon: "🎒" },
  { id: 6, name: "Quemadores", icon: "🏃" },
];

const COMBOS = [
  {
    id: 1,
    name: "Combo Crecimiento Total",
    price: 47600,
    oldPrice: 55000,
    image: "/api/placeholder/300/300",
    tag: "15% OFF",
    rating: 5
  },
  {
    id: 2,
    name: "Pack Definición Extrema",
    price: 38500,
    oldPrice: 42000,
    image: "/api/placeholder/300/300",
    tag: "HOT SALE",
    rating: 4
  },
  {
    id: 3,
    name: "Kit Inicial Gym",
    price: 26500,
    oldPrice: 30000,
    image: "/api/placeholder/300/300",
    tag: "ENVÍO GRATIS",
    rating: 5
  },
  {
    id: 4,
    name: "Dúo Creatina + Shaker",
    price: 21000,
    oldPrice: 25000,
    image: "/api/placeholder/300/300",
    tag: "OFERTA",
    rating: 5
  }
];

const MAS_ELEGIDOS = [
  {
    id: 101,
    name: "Whey Protein 1kg - Vainilla",
    brand: "Star Nutrition",
    price: 18500,
    image: "/api/placeholder/300/300",
    reviews: 120
  },
  {
    id: 102,
    name: "Creatina Monohidrato 300g",
    brand: "ENA Sport",
    price: 22100,
    image: "/api/placeholder/300/300",
    reviews: 85
  },
  {
    id: 103,
    name: "Pre-Entreno C4",
    brand: "Cellucor",
    price: 35000,
    image: "/api/placeholder/300/300",
    reviews: 200
  },
  {
    id: 104,
    name: "Multivitamínico Daily",
    brand: "Universal",
    price: 15400,
    image: "/api/placeholder/300/300",
    reviews: 45
  }
];

const RESENAS = [
  {
    id: 1,
    user: "Maximo T.",
    text: "Excelente precio y llegó super rápido a Salta Capital. El producto 100% original.",
    item: "Whey Protein",
    stars: 5,
    image: "/api/placeholder/150/200"
  },
  {
    id: 2,
    user: "Andrea G.",
    text: "Me asesoraron por WhatsApp antes de comprar. Muy buena atención.",
    item: "Combo Mujer Fit",
    stars: 5,
    image: "/api/placeholder/150/200"
  },
  {
    id: 3,
    user: "Lucas M.",
    text: "Compré el combo de creatina y proteína, la calidad es tremenda.",
    item: "Pack Musculación",
    stars: 5,
    image: "/api/placeholder/150/200"
  }
];

// --- COMPONENTES ---

const ProductCard = ({ product, type = "standard" }) => (
  <div className="group bg-white rounded-none border border-neutral-200 hover:border-black transition-all duration-300 flex flex-col h-full relative">
    {product.tag && (
      <span className="absolute top-0 left-0 bg-black text-white text-[10px] font-bold px-3 py-1 uppercase tracking-wider z-10">
        {product.tag}
      </span>
    )}
    <div className="relative h-56 overflow-hidden bg-white p-6 flex items-center justify-center border-b border-neutral-100">
      <div className="text-center text-neutral-400">
        <img 
            src={product.image} 
            alt={product.name} 
            className="object-contain h-44 w-full group-hover:scale-110 transition-transform duration-500 grayscale group-hover:grayscale-0" 
            onError={(e) => {
                e.target.onerror = null;
                e.target.src = "https://via.placeholder.com/300?text=PUSH+SPORTS";
            }}
        />
      </div>
    </div>
    
    <div className="p-5 flex flex-col flex-grow">
      {product.brand && <p className="text-xs text-neutral-400 font-bold uppercase mb-1 tracking-wide">{product.brand}</p>}
      <h3 className="font-bold text-black text-sm md:text-base mb-2 line-clamp-2 uppercase leading-tight">{product.name}</h3>
      
      <div className="flex items-center mb-3">
        {[...Array(5)].map((_, i) => (
          <Star key={i} size={12} className={`${i < (product.rating || 5) ? "fill-black text-black" : "text-neutral-300"}`} />
        ))}
        {product.reviews && <span className="text-xs text-neutral-400 ml-2 font-medium">({product.reviews} reviews)</span>}
      </div>

      <div className="mt-auto">
        {product.oldPrice && (
          <p className="text-xs text-neutral-400 line-through mb-1 font-mono">${product.oldPrice.toLocaleString()}</p>
        )}
        <div className="flex items-center justify-between">
          <p className="text-xl font-black text-black tracking-tight">${product.price.toLocaleString()}</p>
          <button className="bg-white border-2 border-black text-black p-2 hover:bg-black hover:text-white transition-all duration-300">
            <ShoppingCart size={18} />
          </button>
        </div>
        {type === "combo" && (
            <div className="mt-3 text-[10px] text-neutral-500 font-bold uppercase flex items-center tracking-wide border-t border-neutral-100 pt-2">
                <Truck size={12} className="mr-1" /> Envío gratis Salta
            </div>
        )}
      </div>
    </div>
  </div>
);

const SectionTitle = ({ title, subtitle, dark = false }) => (
  <div className="flex flex-col md:flex-row justify-between items-end mb-10 border-l-8 border-black pl-6">
    <div>
      <h2 className={`text-3xl md:text-4xl font-black uppercase italic tracking-tighter ${dark ? 'text-white border-l-white' : 'text-black'}`}>
        {title}
      </h2>
      {subtitle && <p className={`text-sm mt-2 font-medium tracking-wide ${dark ? 'text-neutral-400' : 'text-neutral-500'}`}>{subtitle}</p>}
    </div>
    <button className={`hidden md:flex items-center text-sm font-bold uppercase tracking-widest mt-4 md:mt-0 group ${dark ? 'text-white' : 'text-black'}`}>
      Ver todos <span className="ml-2 group-hover:translate-x-1 transition-transform">→</span>
    </button>
  </div>
);

const App = () => {
  const [isMenuOpen, setIsMenuOpen] = useState(false);
  const [scrolled, setScrolled] = useState(false);

  useEffect(() => {
    const handleScroll = () => setScrolled(window.scrollY > 20);
    window.addEventListener('scroll', handleScroll);
    return () => window.removeEventListener('scroll', handleScroll);
  }, []);

  return (
    <div className="min-h-screen bg-white font-sans selection:bg-black selection:text-white">
      
      {/* TOP BAR */}
      <div className="bg-neutral-100 text-black text-[10px] md:text-xs font-bold py-2 text-center uppercase tracking-widest border-b border-neutral-200">
        🇦🇷 Envíos a todo Salta en el día | 💳 3 Cuotas sin interés
      </div>

      {/* NAVBAR */}
      <header className={`sticky top-0 z-50 transition-all duration-300 border-b border-neutral-800 ${scrolled ? 'bg-black py-3' : 'bg-black py-6'}`}>
        <div className="container mx-auto px-4 flex justify-between items-center">
          {/* Logo */}
          <div className="flex items-center gap-2">
            <div className="relative group cursor-pointer flex flex-col justify-center">
                <h1 className="text-white font-black text-2xl md:text-4xl italic tracking-tighter leading-none flex items-center">
                    <span className="mr-1 transform -skew-x-12">PUSH</span>
                    <span className="text-white text-base md:text-xl not-italic ml-1 font-light self-center">SPORTS</span>
                </h1>
                <span className="text-neutral-400 text-[10px] md:text-xs tracking-[0.3em] uppercase font-bold text-right -mt-1 mr-1">Salta</span>
            </div>
          </div>

          {/* Desktop Search - Minimalist */}
          <div className="hidden md:flex flex-1 max-w-md mx-12 relative group">
            <input 
              type="text" 
              placeholder="BUSCAR PRODUCTOS..." 
              className="w-full bg-transparent text-white border-b border-neutral-600 py-2 pl-0 pr-8 focus:outline-none focus:border-white transition-colors text-sm uppercase tracking-wide placeholder:text-neutral-600"
            />
            <button className="absolute right-0 top-1/2 -translate-y-1/2 text-neutral-400 group-hover:text-white transition-colors">
              <Search size={18} />
            </button>
          </div>

          {/* Actions */}
          <div className="flex items-center gap-6 text-white">
            <div className="hidden md:flex items-center gap-1 cursor-pointer hover:opacity-70 transition-opacity">
                <div className="text-right mr-2 leading-tight">
                    <p className="text-[10px] text-neutral-400 uppercase">Hola, Atleta</p>
                    <p className="text-xs font-bold uppercase">Ingresar</p>
                </div>
            </div>
            <div className="relative cursor-pointer hover:opacity-70 transition-opacity">
              <ShoppingCart size={24} />
              <span className="absolute -top-1 -right-1 bg-white text-black text-[10px] font-bold h-4 w-4 rounded-full flex items-center justify-center">2</span>
            </div>
            <button className="md:hidden" onClick={() => setIsMenuOpen(!isMenuOpen)}>
              {isMenuOpen ? <X size={24} /> : <Menu size={24} />}
            </button>
          </div>
        </div>
      </header>

      {/* MOBILE MENU */}
      {isMenuOpen && (
        <div className="fixed inset-0 z-40 bg-black pt-24 px-6 md:hidden">
          <nav className="flex flex-col gap-6 text-white text-2xl font-black uppercase italic tracking-tighter">
            <a href="#" className="hover:text-neutral-400 border-b border-neutral-800 pb-4 flex justify-between items-center">Productos <ChevronRight size={20}/></a>
            <a href="#" className="hover:text-neutral-400 border-b border-neutral-800 pb-4 flex justify-between items-center">Combos <ChevronRight size={20}/></a>
            <a href="#" className="hover:text-neutral-400 border-b border-neutral-800 pb-4 flex justify-between items-center">Marcas <ChevronRight size={20}/></a>
            <a href="#" className="hover:text-neutral-400 border-b border-neutral-800 pb-4 flex justify-between items-center">Contacto <ChevronRight size={20}/></a>
          </nav>
        </div>
      )}

      {/* HERO BANNER - SPLIT DESIGN */}
      <section className="relative bg-neutral-900 text-white min-h-[500px] md:h-[600px] flex flex-col md:flex-row">
        
        {/* Left Content */}
        <div className="w-full md:w-1/2 p-8 md:p-16 flex flex-col justify-center relative z-10 bg-black">
          <div className="absolute top-0 left-0 w-full h-1 bg-gradient-to-r from-neutral-800 to-black"></div>
          
          <span className="text-neutral-400 font-bold tracking-[0.2em] text-xs uppercase mb-4 block">
            Suplementación Deportiva
          </span>
          <h2 className="text-5xl md:text-7xl font-black uppercase italic leading-none tracking-tighter mb-6">
            Push <br/>
            <span className="text-white text-stroke">Limits</span>
          </h2>
          <p className="text-neutral-400 text-lg max-w-md mb-8 font-light leading-relaxed">
            Llegamos a Salta para cambiar tu entrenamiento. Las mejores marcas, asesoramiento real y stock permanente.
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <button className="bg-white text-black px-8 py-4 font-black uppercase tracking-widest hover:bg-neutral-200 transition-colors text-sm">
              Ver Catálogo
            </button>
            <button className="border border-white text-white px-8 py-4 font-black uppercase tracking-widest hover:bg-white hover:text-black transition-colors text-sm">
              Nuestros Combos
            </button>
          </div>
        </div>

        {/* Right Image - Placeholder para la foto de la fachada */}
        <div className="w-full md:w-1/2 bg-neutral-800 relative overflow-hidden group">
            {/* NOTA: Aquí iría la "WhatsApp Image 2026-01-27 at 9.32.59 PM (1).jpeg" (Fachada Star Nutrition).
                Como es un prototipo, uso un placeholder gris oscuro.
            */}
            <img 
                src="/api/placeholder/800/800" 
                alt="Local Push Sports Salta" 
                className="w-full h-full object-cover grayscale opacity-60 group-hover:opacity-80 group-hover:scale-105 transition-all duration-700"
            />
            <div className="absolute inset-0 flex items-center justify-center pointer-events-none">
                <span className="bg-black/80 text-white px-4 py-2 text-xs font-mono border border-white/20 backdrop-blur-md">
                    [FOTO FACHADA STAR NUTRITION]
                </span>
            </div>
            
            {/* Location Badge */}
            <div className="absolute bottom-6 left-6 flex items-center gap-2 bg-black px-4 py-2">
                <MapPin size={16} className="text-white" />
                <span className="text-white text-xs font-bold uppercase tracking-wider">Salta Capital</span>
            </div>
        </div>
      </section>

      {/* CATEGORY ICONS - MONOCHROME */}
      <section className="py-12 bg-white border-b border-neutral-100">
        <div className="container mx-auto px-4">
            <div className="flex flex-wrap justify-center md:justify-between gap-8">
                {CATEGORIAS.map((cat) => (
                    <div key={cat.id} className="flex flex-col items-center group cursor-pointer w-24">
                        <div className="w-20 h-20 border-2 border-neutral-100 group-hover:border-black flex items-center justify-center text-3xl transition-all duration-300 rounded-full group-hover:bg-black group-hover:text-white">
                            <span className="group-hover:scale-110 transition-transform">{cat.icon}</span>
                        </div>
                        <span className="mt-4 text-xs font-black uppercase tracking-wider text-neutral-400 group-hover:text-black transition-colors">{cat.name}</span>
                    </div>
                ))}
            </div>
        </div>
      </section>

      {/* FEATURED: PLANT PROTEIN (Foto Chica) */}
      <section className="py-16 bg-neutral-50">
        <div className="container mx-auto px-4">
            <div className="bg-white border border-neutral-200 p-8 md:p-12 flex flex-col md:flex-row items-center gap-12 relative overflow-hidden">
                {/* Decorative background text */}
                <div className="absolute top-0 right-0 text-neutral-100 font-black text-9xl leading-none -mt-4 -mr-4 select-none pointer-events-none italic">
                    PUSH
                </div>

                {/* Image Side - Placeholder para foto con producto */}
                <div className="w-full md:w-1/3 relative aspect-[3/4] md:aspect-square bg-neutral-100 border-2 border-black rotate-2 shadow-2xl">
                     {/* NOTA: Aquí iría la "WhatsApp Image 2026-01-27 at 9.33.00 PM.jpeg" (Chica con Plant Protein).
                    */}
                    <img 
                        src="/api/placeholder/500/600" 
                        alt="Recomendado Push Sports" 
                        className="w-full h-full object-cover grayscale hover:grayscale-0 transition-all duration-500"
                    />
                    <div className="absolute bottom-0 left-0 bg-black text-white px-3 py-1 text-xs font-bold uppercase">
                        Push Team
                    </div>
                </div>

                {/* Content Side */}
                <div className="w-full md:w-2/3 relative z-10">
                    <span className="inline-block border-b-2 border-black pb-1 mb-4 text-xs font-bold uppercase tracking-widest">
                        Recomendado del Mes
                    </span>
                    <h3 className="text-3xl md:text-5xl font-black uppercase italic mb-6 leading-tight">
                        Just Plant Protein <br/> Star Nutrition
                    </h3>
                    <p className="text-neutral-600 mb-6 max-w-lg leading-relaxed">
                        Proteína vegetal de alta calidad. Ideal para quienes buscan una alternativa natural sin lactosa. Textura suave y sabores increíbles. ¡Probada y aprobada por nuestro equipo!
                    </p>
                    <div className="flex items-center gap-4">
                         <span className="text-2xl font-black text-black">$32.500</span>
                         <button className="bg-black text-white px-6 py-3 text-sm font-bold uppercase tracking-wider hover:bg-neutral-800 transition-colors">
                            Comprar Ahora
                         </button>
                    </div>
                </div>
            </div>
        </div>
      </section>

      {/* SECTION 1: COMBOS */}
      <section className="py-16 container mx-auto px-4">
        <SectionTitle title="Combos Push" subtitle="Packs armados para maximizar resultados" />
        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-x-4 gap-y-8">
          {COMBOS.map((product) => (
            <ProductCard key={product.id} product={product} type="combo" />
          ))}
        </div>
      </section>

      {/* SECTION 2: BEST SELLERS - DARK MODE */}
      <section className="py-24 bg-black text-white">
        <div className="container mx-auto px-4">
            <SectionTitle title="Los Más Elegidos" subtitle="Top ventas en Salta este mes" dark={true} />
            <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-x-4 gap-y-8">
                {MAS_ELEGIDOS.map((product) => (
                    <ProductCard key={product.id} product={product} />
                ))}
            </div>
        </div>
      </section>

      {/* BENEFITS BAR - MONOCHROME */}
      <section className="bg-neutral-100 py-12 border-t border-neutral-200">
        <div className="container mx-auto px-4 grid grid-cols-2 md:grid-cols-4 gap-8">
            <div className="flex flex-col items-center text-center group">
                <Truck className="mb-4 text-black group-hover:scale-110 transition-transform" size={36} strokeWidth={1.5} />
                <h4 className="font-black uppercase text-black text-sm tracking-widest mb-1">Envío Rápido</h4>
                <p className="text-[10px] text-neutral-500 uppercase tracking-wide">Salta Capital y Alrededores</p>
            </div>
            <div className="flex flex-col items-center text-center group">
                <CreditCard className="mb-4 text-black group-hover:scale-110 transition-transform" size={36} strokeWidth={1.5} />
                <h4 className="font-black uppercase text-black text-sm tracking-widest mb-1">3 Cuotas</h4>
                <p className="text-[10px] text-neutral-500 uppercase tracking-wide">Sin interés con tarjetas</p>
            </div>
            <div className="flex flex-col items-center text-center group">
                <ShieldCheck className="mb-4 text-black group-hover:scale-110 transition-transform" size={36} strokeWidth={1.5} />
                <h4 className="font-black uppercase text-black text-sm tracking-widest mb-1">Garantía</h4>
                <p className="text-[10px] text-neutral-500 uppercase tracking-wide">Productos 100% Originales</p>
            </div>
            <div className="flex flex-col items-center text-center group">
                <Zap className="mb-4 text-black group-hover:scale-110 transition-transform" size={36} strokeWidth={1.5} />
                <h4 className="font-black uppercase text-black text-sm tracking-widest mb-1">Asesoramiento</h4>
                <p className="text-[10px] text-neutral-500 uppercase tracking-wide">Personalizado por expertos</p>
            </div>
        </div>
      </section>

      {/* REVIEWS & VIDEOS GRID */}
      <section className="py-20 container mx-auto px-4">
        <div className="grid md:grid-cols-3 gap-12">
            
            {/* Reviews Column */}
            <div className="md:col-span-1">
                <h3 className="text-2xl font-black uppercase italic mb-8 border-b-2 border-black pb-2 inline-block">
                    Push Community
                </h3>
                <div className="space-y-6">
                    {RESENAS.map((review) => (
                        <div key={review.id} className="bg-white p-6 border border-neutral-200 shadow-[4px_4px_0px_0px_rgba(0,0,0,1)] flex gap-4">
                             <div className="w-10 h-10 bg-neutral-200 rounded-full flex-shrink-0 overflow-hidden grayscale">
                                <img src={review.image} className="w-full h-full object-cover" alt="User" />
                             </div>
                             <div>
                                 <div className="flex mb-2">
                                    {[...Array(5)].map((_, i) => (
                                        <Star key={i} size={10} className="fill-black text-black" />
                                    ))}
                                 </div>
                                 <p className="text-sm text-neutral-600 mb-3 italic">"{review.text}"</p>
                                 <p className="text-xs font-bold text-black uppercase tracking-wide">{review.user}</p>
                             </div>
                        </div>
                    ))}
                </div>
            </div>

            {/* Videos/Info Column */}
            <div className="md:col-span-2">
                 <h3 className="text-2xl font-black uppercase italic mb-8 border-b-2 border-black pb-2 inline-block flex items-center gap-2">
                    <Play size={20} className="fill-black"/> Push TV
                </h3>
                <div className="grid grid-cols-2 md:grid-cols-3 gap-4 h-full">
                    {[1, 2, 3].map((v) => (
                        <div key={v} className="relative group cursor-pointer overflow-hidden bg-black h-72">
                             <img src="/api/placeholder/400/600" className="w-full h-full object-cover opacity-60 group-hover:opacity-40 transition-opacity grayscale" alt="Video cover" />
                             <div className="absolute inset-0 flex flex-col items-center justify-center p-4">
                                 <div className="bg-white p-3 rounded-full mb-4 group-hover:scale-110 transition-transform">
                                     <Play className="text-black fill-black" size={20} />
                                 </div>
                                 <p className="text-white font-bold text-center text-xs uppercase tracking-wider border-t border-white/20 pt-2 w-full">
                                    Tips de Entrenamiento #{v}
                                 </p>
                             </div>
                        </div>
                    ))}
                </div>
            </div>

        </div>
      </section>

      {/* FOOTER - MINIMALIST BLACK */}
      <footer className="bg-black text-white pt-20 pb-10 border-t border-neutral-900">
        <div className="container mx-auto px-4 grid md:grid-cols-4 gap-12 mb-16">
            <div className="md:col-span-1">
                <h2 className="text-3xl font-black italic mb-6 tracking-tighter">PUSH SPORTS <span className="text-neutral-500">SALTA</span></h2>
                <p className="text-neutral-500 text-sm mb-6 leading-relaxed">
                    Somos el combustible de los atletas de Salta. Suplementación inteligente y equipamiento de primer nivel.
                </p>
                <div className="flex gap-4">
                    <div className="bg-neutral-900 p-3 hover:bg-white hover:text-black transition-colors cursor-pointer border border-neutral-800">
                        <Instagram size={18} />
                    </div>
                    <div className="bg-neutral-900 p-3 hover:bg-white hover:text-black transition-colors cursor-pointer border border-neutral-800">
                        <MessageCircle size={18} />
                    </div>
                </div>
            </div>

            <div>
                <h4 className="font-bold uppercase mb-6 text-white text-xs tracking-[0.2em]">Shop</h4>
                <ul className="space-y-3 text-sm text-neutral-500">
                    <li className="hover:text-white cursor-pointer transition-colors">Proteínas</li>
                    <li className="hover:text-white cursor-pointer transition-colors">Creatinas</li>
                    <li className="hover:text-white cursor-pointer transition-colors">Quemadores</li>
                    <li className="hover:text-white cursor-pointer transition-colors">Combos</li>
                </ul>
            </div>

            <div>
                <h4 className="font-bold uppercase mb-6 text-white text-xs tracking-[0.2em]">Soporte</h4>
                <ul className="space-y-3 text-sm text-neutral-500">
                    <li className="hover:text-white cursor-pointer transition-colors">Envíos</li>
                    <li className="hover:text-white cursor-pointer transition-colors">Pagos</li>
                    <li className="hover:text-white cursor-pointer transition-colors">Devoluciones</li>
                    <li className="hover:text-white cursor-pointer transition-colors">FAQ</li>
                </ul>
            </div>

            <div>
                <h4 className="font-bold uppercase mb-6 text-white text-xs tracking-[0.2em]">Únete</h4>
                <div className="flex border-b border-neutral-800 pb-2">
                    <input type="email" placeholder="TU EMAIL" className="bg-transparent text-white text-sm w-full focus:outline-none placeholder:text-neutral-700 uppercase" />
                    <button className="text-white font-bold uppercase text-xs hover:text-neutral-400 transition-colors">
                        ENVIAR
                    </button>
                </div>
            </div>
        </div>
        <div className="container mx-auto px-4 text-center md:text-left text-neutral-700 text-[10px] uppercase tracking-wider flex flex-col md:flex-row justify-between items-center">
            <p>© 2026 PUSH SPORTS SALTA. TODOS LOS DERECHOS RESERVADOS.</p>
            <p className="mt-2 md:mt-0">DISEÑADO PARA GANAR</p>
        </div>
      </footer>

      {/* WHATSAPP FLOATING BUTTON - Black Theme */}
      <a 
        href="https://wa.me/" 
        target="_blank" 
        rel="noopener noreferrer"
        className="fixed bottom-8 right-8 bg-black text-white p-4 rounded-full shadow-[0_0_20px_rgba(0,0,0,0.3)] hover:scale-110 transition-all z-50 flex items-center justify-center border border-neutral-800 group"
      >
        <MessageCircle size={28} />
        <span className="absolute right-full mr-4 bg-black text-white text-[10px] font-bold px-3 py-2 uppercase tracking-wider shadow-xl whitespace-nowrap hidden group-hover:block border border-neutral-800">
            Chat Directo
        </span>
      </a>

    </div>
  );
};

export default App;